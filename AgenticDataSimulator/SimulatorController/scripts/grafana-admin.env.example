# Copy to scripts/grafana-admin.env (gitignored).
# Grafana server-admin credentials for user provisioning (not the dashboard API key).
GRAFANA_ADMIN_USER="admin"
GRAFANA_ADMIN_PASSWORD="your-grafana-admin-password"
