# Copy to scripts/grafana-user-passwords.env (gitignored via .env* in SimulatorController).
# One line per Controller user — Grafana password must match their Controller login password.
#
# arne=their-controller-password
# arneme=their-controller-password
