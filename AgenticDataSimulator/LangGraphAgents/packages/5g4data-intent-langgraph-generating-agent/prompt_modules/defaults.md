Defaults policy:
- `imo:handler` is fixed to `inServ`.
- `imo:owner` is fixed to `inChat`.
- Auto-generate `dct:description` when user does not provide one.
- If required details are missing, ask the smallest possible clarification.
