apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "inserv.fullname" . }}-config
  namespace: {{ include "inserv.namespace" . }}
data:
{{- range $key, $value := .Values.env }}
  {{ $key }}: "{{ $value }}"
{{- end }}

